<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1024 1024">
  <!-- Tile: mdreader's own accent color (src/styles.css) -->
  <rect x="64" y="64" width="896" height="896" rx="200" ry="200" fill="#3568d4"/>

  <!-- Monoline "MD" glyph, stroked paths only (no text element — must
       rasterize without depending on system font resolution). -->
  <g fill="none" stroke="#ffffff" stroke-width="110" stroke-linecap="round" stroke-linejoin="round">
    <path d="M201,742 L201,282 L351,535 L501,282 L501,742"/>
    <path d="M623,282 L701,282 A122,230 0 0 1 701,742 L623,742 Z"/>
  </g>

  <!-- Pupil inside the D's counter: "MD" at small sizes, "MD + eye" at large. -->
  <circle cx="725" cy="512" r="42" fill="#ffffff"/>
</svg>
